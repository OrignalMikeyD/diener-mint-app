{
  "name": "diener-mint-app-blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat workspace for the diener-mint-app ERC-721 contract (CRCP 6340 Module 10)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
